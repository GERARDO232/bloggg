<h2>Articulo</h2>

<%= form_with(model: @article, local: true) do |f| %>
  <div>
    <%= form.label :title%>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>

<% end %>
